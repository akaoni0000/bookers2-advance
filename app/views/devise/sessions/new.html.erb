<div class= "in_up">
<h2>Log in</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %> <!--resourcenにはモデルが入る、表示画面のurlのモデル名が入る-->
  <div class="field">
    <%= f.label :Name %><br />
    <%= f.text_field :name, autofocus: true, autocomplete: "name" %> <!--autofocusは最初に合わせられるカーソルすなわち最初にメールのフォームにカーソルが合わせられる autocompleateは以前入力したものを予測変換させる-->
  </div>

  <div class="field">
    <%= f.label :Password %><br />
    <%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Log in" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>  <!--部分テンプレートを呼び出すdeviceフォルダのshareのlinkを呼び出す,部分テンプレートのファイルは最初に_がつく_links.html.erb-->
<p class="copyright">CopyRight Infratop.inc</p>
</div>